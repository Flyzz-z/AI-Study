# 深度学习
## 
### Adam优化器
Adam（Adaptive Moment Estimation）同时维护梯度的一阶矩（动量）与二阶矩（未中心方差），用指数滑动平均追踪历史信息，并为每个参数自适应调整步长。训练初期对估计值做偏置校正，避免初始值过小。常见超参数：学习率 `lr`、一阶衰减 `β1=0.9`、二阶衰减 `β2=0.999`、数值稳定常数 `ε=1e-8`。
参数更新公式如下：

1. 一阶矩（动量）估计：
   $$m_t = \beta_1 \cdot m_{t-1} + (1 - \beta_1) \cdot g_t$$

2. 二阶矩（未中心方差）估计：
   $$v_t = \beta_2 \cdot v_{t-1} + (1 - \beta_2) \cdot g_t^2$$

3. 偏置校正：
   $$\hat{m}_t = \frac{m_t}{1 - \beta_1^t}, \quad \hat{v}_t = \frac{v_t}{1 - \beta_2^t}$$

4. 参数更新：
   $$\theta_{t+1} = \theta_t - \frac{\eta}{\sqrt{\hat{v}_t} + \epsilon} \cdot \hat{m}_t$$

其中：
- $g_t$ 为第 $t$ 步的梯度
- $m_t$、$v_t$ 分别为第 $t$ 步的一阶和二阶矩估计
- $\eta$ 为学习率
- $\epsilon$ 为数值稳定常数

一阶矩表示方向，二阶矩表示大小，梯度越大，更新步长越小。


